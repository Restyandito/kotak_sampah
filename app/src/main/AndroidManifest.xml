<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplikasi_layanan_sampah"
        tools:targetApi="31">

        <!-- Set LoginActivity sebagai aktivitas utama -->
        <activity android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".JadwalPengambilanActivity" />
        <activity android:name=".EdukasiPenggunaActivity" />
        <activity android:name=".OrganikActivity" />
        <activity android:name=".NonOrganikActivity" />
        <activity android:name=".B3Activity" />
        <activity android:name=".InputSampahActivity">

        </activity>
    </application>
</manifest>
